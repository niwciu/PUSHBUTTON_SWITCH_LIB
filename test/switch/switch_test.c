#include "unity/fixture/unity_fixture.h"

// #include "tested_module.h"

TEST_GROUP(Switch);

TEST_SETUP(Switch)
{
    /* Init before every test */
}

TEST_TEAR_DOWN(Switch)
{
    /* Cleanup after every test */
}

TEST(Switch, FirstTest)
{
    TEST_FAIL_MESSAGE("Implement your test!");
}
